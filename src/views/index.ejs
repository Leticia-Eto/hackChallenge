<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <title>Verificação de Inscrição</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-5">
  <h1>Verificação de Inscrição</h1>
  
  <form action="/verificar" method="POST">
    <div class="mb-3">
      <label for="codigo" class="form-label">Código de Inscrição</label>
      <input type="text" id="codigo" name="codigo" class="form-control" required>
    </div>

    <div class="mb-3">
      <label for="palestraId" class="form-label">Escolha a Palestra</label>
      <select class="form-select" name="palestraId" required>
        <% palestras.forEach(palestra => { %>
          <option value="<%= palestra.id %>"><%= palestra.nome %></option>
        <% }) %>
      </select>
    </div>

    <button class="btn btn-primary">Verificar Inscrição</button>
  </form>

</body>
</html>
